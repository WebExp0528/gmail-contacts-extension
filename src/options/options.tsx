/* global document */

import React from 'react';
import ReactDOM from 'react-dom';

const Index = () => <div>This is Gmail Contacts Extension Option Page!</div>;

ReactDOM.render(<Index />, document.getElementById('display-container'));
