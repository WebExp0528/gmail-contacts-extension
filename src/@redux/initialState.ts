import user from './user/initialState';
import authenticated from './authenticated/initialState';
import contacts from './contacts/initialState';

export const initialState = {
    user,
    authenticated,
    contacts,
};
